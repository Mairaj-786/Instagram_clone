import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  titleText: {
    fontSize: 16,
    color: "#000",
    opacity: 0.8,
  },
  titleTextBold: {
    fontSize: 16,
    color: "#000",
    opacity: 0.8,
    fontWeight: "bold",
  },
});
